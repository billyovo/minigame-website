export enum RecordMode {
    RECORD = 'record',
    COUNT = 'count',
}

export enum Server {
    ALL = 'all',
    SURVIVAL = 'survival',
    SKYBLOCK = 'skyblock',
}

export enum ServerChinese {
    ALL = '全部',
    SURVIVAL = '生存',
    SKYBLOCK = '空島',
}

export enum ServerColour {
    SURVIVAL = 'rgb(46,204,113)',
    SKYBLOCK = 'rgb(52,125,219)'
}
